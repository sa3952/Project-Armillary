# Dependency policy

Production and build dependencies are selected separately, pinned and hashed；pyswisseph 2.10.3.2 is
built from its exact source distribution. Each release verifies locks、SBOM、fresh vulnerability data、
third-party source／licenses and any accepted unresolved vulnerability. Scanner counts are evidence for
triage, not proof of exploitability or safety.
